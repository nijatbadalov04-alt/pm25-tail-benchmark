# Normalize text line endings on commit; check out platform-native. Silences Windows LF/CRLF warnings.
* text=auto

# Binary assets — never normalize or diff as text.
*.parquet binary
*.pdf binary
*.pkl binary
*.png binary
*.npz binary
